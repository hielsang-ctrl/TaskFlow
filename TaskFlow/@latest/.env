# GitHub Personal Access Token (optional but recommended)
# Without this, GitHub limits you to 60 API requests/hour
# Generate one at: https://github.com/settings/tokens
# Only needs the "public_repo" read scope for public repositories
VITE_GITHUB_TOKEN=
